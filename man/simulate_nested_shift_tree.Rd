% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_trees.R
\name{simulate_nested_shift_tree}
\alias{simulate_nested_shift_tree}
\title{Simulate a tree with a nested rate shift}
\usage{
simulate_nested_shift_tree(
  n_background,
  n_outer,
  n_inner,
  lambda_background = 1,
  outer_ratio = 3,
  inner_ratio = 6,
  epsilon = 0.2,
  rho = 1
)
}
\arguments{
\item{n_background}{Background tree size (tip count).}

\item{n_outer}{Tips in the outer shifted clade before the inner graft.}

\item{n_inner}{Tips in the inner (nested) shifted clade.}

\item{lambda_background}{Background speciation rate.}

\item{outer_ratio}{Outer clade speciation rate as a multiple of background.}

\item{inner_ratio}{Inner clade speciation rate as a multiple of background.}

\item{epsilon}{Extinction fraction.}

\item{rho}{Sampling fraction.}
}
\value{
A list with the tree, the outer clade's tip set, and the inner
clade's tip set (a subset of the outer set).
}
\description{
Builds a tree with one rate shift nested inside another: an outer clade
with its own diversification rate that itself contains an inner clade
with a further rate change. This is the harder case for shift detection,
since the inner and outer regimes must be attributed correctly at once.
}
\details{
The inner shift is grafted onto a tip of the outer shifted clade, so the
nesting is real by construction. The two clades' tips are labeled so the
inner tips are a strict subset of the outer clade's descendants.
}
