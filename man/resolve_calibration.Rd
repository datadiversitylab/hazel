% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve_calibration.R
\name{resolve_calibration}
\alias{resolve_calibration}
\title{Resolve which calibration table to use}
\usage{
resolve_calibration(calibration = NULL, verbose = TRUE)
}
\arguments{
\item{calibration}{A data frame, a file path, or \code{NULL}.}

\item{verbose}{Whether to print which source was used.}
}
\value{
A calibration data frame, or \code{NULL} if none is available.
}
\description{
Returns a calibration table following a precedence order: a table you
pass in (a data frame, or a path to a saved \code{.rds}/\code{.rda}
file) always wins; otherwise the table shipped with the package is used;
otherwise \code{NULL} is returned so the caller can calibrate on the fly.
}
